# Server-only credentials — never commit
api/config.local.php
api/write-config-once.php

# Deploy output (generated by scripts/deploy-pack.ps1)
dist/

# Node build tooling
node_modules/

# Local archives (use dist/triniti-deploy-*.zip instead)
*.zip
